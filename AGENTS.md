# Prompt Halo 项目约束

- 用中文说明修改、验证结果和未完成项。
- 保留无关修改；提交、推送和发布需属于用户授权范围。
- Ctrl + Alt + Q 是当前已选择的快捷键；不擅自更换。
- 用户于 2026-09-14 验证准确输入：两个浏览器、微信、QQ、Photoshop 文字输入和 Typora，包含不同输入框位置。
- 输入基线标签：input-baseline-efe43e268ac6。性能或视觉优化不得顺带重构已验收输入链路。
- 保留呼出时捕获目标、前台归属校验、恢复原窗口与焦点、隐藏菜单后再次确认焦点、单次粘贴、防重复和过期菜单保护。禁止通过重新点击鼠标位置猜测输入框。
- 保留透明且 focusable:true 的窗口与 showInactive 唤起；不要为了提速删除焦点检查。
- 修改输入代码时必须解释原因并运行适用的原生回归；仅 DOM 点击、剪贴板赋值或 SendInput 成功不能证明目标应用已输入。
- 不提交用户词库、用户数据目录、运行日志、凭据、依赖或本机诊断清单。
