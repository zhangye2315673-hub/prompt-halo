# 用户验收基线 · 2026-09-14

版本：efe43e268ac6。快捷键：Ctrl + Alt + Q。
用户明确确认：两个浏览器、微信、QQ、Photoshop 文字输入、Typora，以及不同输入框位置，都可准确插入。
这是用户实测验收结果，不是本轮代理逐应用重测。

保护约束：保留调用时捕获目标、选词后确认前台归属、恢复原窗口与原焦点、隐藏菜单、再次确认焦点、单次粘贴、取消和重复输入保护。
后续唤起性能优化不得删除检查、以鼠标重新点击猜测输入框或改动底层输入服务。保留 focusable:true + showInactive。

本目录是只读用途的源文件快照；sha256.json 用于完整性核对。恢复时只覆盖快照列出的程序文件，不覆盖用户词库或用户数据目录。
