# 圆环界面改版 · 2026-09-14

## 范围

移除主工作台窗口和 DOM；保留一个透明、可点击、showInactive 唤起的圆环窗口。
四个圆按钮与持续显示的中心小圆点；移除双轨道线和正常状态底部文字。
一级入口点击位置作为二级定位中心，短条收拢环绕，最多 8 项分页。
分类内新增，短条右键编辑；支持自定义分类、后缀、收藏、复制、确认删除。原存储键与加载页面路径保留。

## 已验证

- 原生 windows-input 服务文件未改变；恢复原窗口、隐藏后确认焦点、单次粘贴函数未改变。
- 菜单生命周期：立即显示、目标采集延迟、取消、重开、过期请求、重复插入、外部前台拒绝。
- 真实 Electron 渲染器：四个按钮、小点尺寸、二级中心坐标、短条无重叠/越界、透明像素、新增/编辑/复制/删除、分类/后缀/收藏、搜索/退格光标、分页、空白关闭。
- 原生 Chrome 临时 contenteditable：真实 Ctrl+Alt+Q 和真实鼠标选择，准确在 BEFORE/AFTER 之间单次插入，旁边字段保持不变，重开与 Esc 通过。

## 尚未完成

搜索模式原生端到端测试先发现搜索光标重置，已修复并通过真实渲染器的字符/退格测试。之后原生复测因前台窗口变化/测试区域被干扰中止，未将搜索模式跨应用回写标为通过。
这轮未重测实际微信、QQ、Photoshop、Typora；此前用户验收不是新版逐软件验证。
不同缩放、多显示器和长期闲置后的性能仍需用户现场确认。

本次改动保存在本地工作目录，未提交或推送 GitHub。
